minimumReleaseAge: 4320
# else pnpm re-checks lockfile entries against the age limit and a
# dependabot security bump fails --frozen-lockfile until it ages out
trustLockfile: true

allowBuilds:
  esbuild: true
  unrs-resolver: true
  sharp: true
