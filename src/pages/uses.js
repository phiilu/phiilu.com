import Layout from '@components/Layout/Layout';
import Head from '@components/Head/Head';

const Uses = () => {
  return (
    <>
      <Head title="Uses" />
      <Layout>
        <h1>Uses</h1>
      </Layout>
    </>
  );
};

export default Uses;
